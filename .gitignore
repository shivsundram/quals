# Regenerable data — fetched/generated by bench/fetch_*.py and bench/gen_data.py.
bench/data/
bench/data_real/
bench/data_genomes/

# SLURM job logs (may grow large; useful only ephemerally).
bench/sbatch/logs/

# Python bytecode.
__pycache__/
*.pyc
*.pyo

# Editor / OS junk.
.DS_Store
.vscode/
.idea/
*.swp
*~

# bench results: KEEP committed (small JSON+CSV, real research history).
# (no entry needed — they're tracked.)
